export const PageInstructure = ({heading}) => {
    return (
        <div className="text-center py-5 bg-bgOrangeColorLight">
            <h2 className="text-lg text-textColorBlack font-bold">{heading}</h2>
        </div>
    )
}